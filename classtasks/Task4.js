const product = {
        price: "1200",
        inStock: "true",
        }

product.price = 1000
product.inStock = false
product.price = 500
        
const prompt = require("prompt-sync")();
const user_price = ("Enter the price")
const user_stock = ("Enter true / false")

    if (product.inStock == false){
       const reponse = "Out of stock"
             console.log(response)
        }
    if (product.price > 1000){
        const reply == "Expensive"
             console.log(reply)
        } else {
        (console.log("Affordable")) 
        }
